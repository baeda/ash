func main() : void
{
    dump 0 * 0;
    dump 0 * 1;
    dump 1 * 0;
    dump 1 * 1;
    dump 2 * 2;
    dump 2 * 2 * 2;
    dump 2 * 2 * 2 * 2;
    dump 2 * 2 * 2 * 2 * 2;
    dump 2 * 2 * 2 * 2 * 2 * 2;
}
